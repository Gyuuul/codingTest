function solution(rsp) {
  var answer = "";
  let str= String(rsp);
  for (let i = 0; i < arr.length; i++) {
    if (str[i] === "0") answer += "5";
    if (str[i] === "2") answer += "0";
    if (str[i] === "5") answer += "2";
  }
  return answer;
}
