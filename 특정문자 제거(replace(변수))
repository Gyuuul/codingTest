function solution(my_string, letter) {
    var regexAllCase = new RegExp(letter, "g")
    var answer = my_string.replace(regexAllCase,"");
    return answer;
}
// var regexAllCase = new RegExp(letter, "gi")
// str = str.replace(regexAllCase,"");
